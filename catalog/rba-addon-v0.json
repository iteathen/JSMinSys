{
  "version": "0.6.0",
  "status": "runtime-configured-hot-addon",
  "modules": [
    "addons/rba-tt32.mjs",
    "addons/rba-connect4-geometry.mjs",
    "addons/rba-connect4-coordinate.mjs",
    "addons/rba-connect4-front.mjs",
    "addons/rba-connect4-solver.mjs",
    "addons/rba-connect4-profile.mjs",
    "addons/cpc-connect4.mjs",
    "addons/rba-connect4-alphabeta.mjs"
  ],
  "geometryContract": {
    "configurationTime": [
      "columns",
      "rows",
      "optional actionOrder"
    ],
    "derived": [
      "cellCount",
      "winning-line count",
      "residual-shape vocabulary",
      "basisCapacity",
      "coordWords",
      "keyWords",
      "edgeCapacity",
      "reflection",
      "action priority"
    ],
    "supportRepresentation": "one uint32 height per configured column in q identity; no 3-bit row limit",
    "limits": "Only explicit JS/TypedArray/uint32 profile limits validated during initialization; no proving-workload board dimensions are authoritative."
  },
  "ttContract": {
    "configured": [
      "keyWords",
      "basisCapacity",
      "edgeCapacity"
    ],
    "owns": [
      "exact q identity",
      "immutable basis payload",
      "generation/ref/execution lifetime",
      "lower/upper exact evidence",
      "dependency topology",
      "ready/event membership",
      "recycling"
    ]
  },
  "solverContract": {
    "owns": [
      "Connect4 residual cofactor semantics",
      "support-local basis transport",
      "reflection canonicalization",
      "conservative CPC/NDC exact certificates and bounds",
      "CPC-first exact negamax/alpha-beta traversal",
      "optional four-front construction/query reference refinement",
      "RBA-native child preparation",
      "generic min/max reconciliation glue"
    ],
    "excludes": [
      "physical-board reconstruction after ingress",
      "fixed board dimension",
      "unqualified parity ownership as W/D/L authority",
      "alpha/beta search windows as shared exact q evidence",
      "proof/certificate identity"
    ]
  },
  "qualification": [
    "4x4 coordinate/front/solve controls",
    "10x10 coordinate/front controls",
    "10x10 derived carrier has 10 edges and runtime-computed key/basis widths",
    "fast-profile versus zero-specialization-budget equivalence on configured board",
    "CPC per-column XOR parity checked against literal event enumeration on 4x4 and 10x10",
    "current-player immediate terminal supersedes an opponent fork in exact CPC control",
    "distinct playable singleton fork closes as exact loss for side to move",
    "CPC-only and CPC+Four-Front alpha-beta match independent exact oracles on 4x4 and late/rank-28 7x6 controls",
    "A/B benchmark records Four-Front node reduction separately from recursive front-step/runtime cost",
    "pooled-frontier paired response qualified with exact positive/negative 4x4 oracle controls",
    "fixed L=1 synchronized-frontier pair blocker qualified as an opt-in CPC extension",
    "rank-16+ prefixes of maintained standard-7x6 benchmark fixtures scanned with zero frontier-extension interval/kind changes",
    "pooled-frontier plus deterministic maximal synchronized-channel response qualified as an opt-in CPC extension with independent oracle controls",
    "corrected rank-16+ standard-7x6 prefix A/B scan found zero interval/kind changes from the frontier-response extension",
    "forced-block and universal support-lift terminal-response closures match independent 4x4 exact oracles",
    "support-lift closure reduces the maintained unresolved standard-7x6 rank-28 control from 54 to 49 CPC-first alpha-beta nodes without another basis scan"
  ],
  "specializationContract": {
    "selection": "cold initialization only",
    "selector": "prepareConnect4RbaExecutionProfile",
    "currentChoices": [
      "dense vs sparse residual cell-removal lookup",
      "dense vs sparse residual subset lookup",
      "3-word vs runtime-span coordinate permutation",
      "6-word vs runtime-span skyline insertion/product"
    ],
    "memoryPolicy": "dense geometry tables are admitted only when they fit the initialization specializationBudgetBytes",
    "hotPolicy": "selected functions are invoked directly by the prepared evaluator/front arena; no recurring width/height branch chooses the implementation",
    "correctness": "every specialization must remain semantically equivalent to the runtime-configured general path"
  },
  "cpcAlphaBetaContract": {
    "primaryUnresolvedPath": "CPC/NDC exact closure -> fixed initialization-time action order -> one-ply RBA cofactor -> exact negamax/alpha-beta",
    "temporalPolicy": "ordinary first-win order is supplied by traversal/negamax; explicit temporal metadata is required only when a CPC certificate jumps across intervening play",
    "exactCpc": [
      "terminal state",
      "one/both-sided residual exhaustion interval",
      "guarded all-even paired-response no-win interval",
      "current-player immediate terminal",
      "distinct playable opponent singleton fork",
      "forced single immediate block",
      "forced-block support-lift terminal response",
      "universal legal-move support-lift terminal response"
    ],
    "projectedOnly": [
      "future singleton CPC owner from XOR of per-column event contributions",
      "future CPC fork pairs whose response/race guards are not yet closed"
    ],
    "cachePolicy": "Only globally exact q values are stored as exact cache entries. Narrow alpha/beta returns remain local search bounds.",
    "moveOrderPolicy": "CPC-only mode uses immutable initialization-prepared actionOrder and no per-depth action-order scratch.",
    "optionalLongRange": [
      "pooled-frontier paired-response no-win interval",
      "deterministic L=1 synchronized-frontier pair blockers",
      "deterministic maximal synchronized channel over ascending odd-remainder column pairs",
      "selected at initialization and currently opt-in because the corrected maintained standard-7x6 prefix scan shows no additional interval/kind closures"
    ],
    "frontierResponsePolicy": "The existing all-even paired-response theorem remains the default production path. Pooled plus synchronized-channel response is qualified but opt-in; support-lift terminal response is part of the production CPC tactical closure."
  },
  "fourFrontRole": {
    "status": "qualified-experimental-reference",
    "purpose": "A/B comparison and symbolic-front research; not the primary unresolved fallback",
    "reason": "Recursive Four-Front can reduce alpha-beta nodes but adds nested symbolic recursion and per-depth action-bound retention.",
    "measuredControls": {
      "unresolvedStandard7x6": {
        "alphaBetaNodes": 54,
        "fourFrontAlphaBetaNodes": 37,
        "alphaBetaMs": 0.27128299999999683,
        "fourFrontAlphaBetaMs": 6.743407999999988,
        "fourFrontSteps": 3703,
        "note": "single GitHub-runner control; directional evidence, not a universal performance claim",
        "currentProductionAlphaBetaNodes": 49,
        "currentProductionNote": "After support-lift CPC closure, the maintained rank-28 control uses 49 alpha-beta nodes; the original Four-Front A/B comparison used 54 baseline nodes before that closure."
      }
    }
  }
}
